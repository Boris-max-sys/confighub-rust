[package]
name = "confighub"
version = "0.1.0"
edition = "2021"

[dependencies]
async-std = { version = "1.12", features = ["attributes"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
regex = "1.10"

[dev-dependencies]
tempfile = "3.8"